apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wipp-tensorboard
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
spec:
  tls:
    - hosts:
      - TENSORBOARD_HOSTNAME_VALUE
      secretName: WIPP_TLS_SECRET
  rules:
  - host: TENSORBOARD_HOSTNAME_VALUE
    http:
      paths:
      - path: "/"
        pathType: Prefix
        backend:
          service:
            name: wipp-tensorboard
            port: 
              number: 6006
